name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build, lint, and test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      VITE_API_URL: http://localhost:5236/api

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type-check
        run: npx vue-tsc --noEmit

      - name: Build
        run: npm run build-only

      - name: Run unit & integration tests with coverage
        run: npm run test:coverage

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          if-no-files-found: ignore
          retention-days: 14

  e2e-tests:
    name: E2E Tests (mocked)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      VITE_API_URL: http://localhost:5236/api

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run E2E tests
        run: npm run test:e2e

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-mocked
          path: playwright-report/
          if-no-files-found: ignore
          retention-days: 14

  e2e-integration:
    name: E2E Integration (Docker)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    if: ${{ vars.RUN_E2E == 'true' }}
    env:
      SA_PASSWORD: ${{ secrets.SA_PASSWORD }}
      VITE_API_URL: http://localhost:5236/api

    steps:
      - name: Checkout Vue repo
        uses: actions/checkout@v4

      - name: Checkout .NET API repo
        uses: actions/checkout@v4
        with:
          repository: rvnminers-A-and-N/sunroom-crm-dotnet
          path: dotnet-api

      - name: Start API via docker-compose
        working-directory: dotnet-api
        run: docker compose up -d --build

      - name: Wait for API to be ready
        run: |
          for i in $(seq 1 60); do
            if curl -s -o /dev/null --max-time 3 http://localhost:5236/; then
              echo "API is ready"
              exit 0
            fi
            sleep 3
          done
          echo "API failed to start"
          docker compose -f dotnet-api/docker-compose.yml logs
          exit 1

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npm run test:e2e

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-integration
          path: playwright-report/
          if-no-files-found: ignore
          retention-days: 14

      - name: Stop API containers
        if: always()
        working-directory: dotnet-api
        run: docker compose down -v
